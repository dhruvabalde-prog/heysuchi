"use client";
import {useEffect,useState}from"react"; import {useRouter}from"next/navigation"; import type {Mission}from"../../lib/types"; import styles from"./page.module.css";
export default function Missions(){const [missions,setMissions]=useState<Mission[]>([]);const [loading,setLoading]=useState(true);const router=useRouter();useEffect(()=>{fetch("/api/mission",{cache:"no-store"}).then(r=>r.json()).then(d=>setMissions(d.missions??[])).catch(()=>{}).finally(()=>setLoading(false));},[]);return <main className={styles.page}><a href="/" className={styles.back}>← Today</a><p className="eyebrow">MISSIONS</p><h1>Everything in motion.</h1><p className={styles.lead}>Your outcomes, without the task-management overhead.</p><div className={styles.list}>{missions.map(m=><button className={styles.card} key={m.id} onClick={()=>{localStorage.setItem("heysuchi:last-mission",JSON.stringify(m));router.push("/mission")}}><span className={styles.icon}>{m.domain==="Life"?"⌂":"✦"}</span><span><strong>{m.title}</strong><small>{m.domain} · {m.status.replace("_"," ")} · {m.progress}%</small></span></button>)}</div>{loading&&<div className={styles.empty}>Loading missions…</div>}{!loading&&!missions.length&&<div className={styles.empty}>No missions yet. Tell Suchi what you want done.</div>}</main>}
